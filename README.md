# C++ Learning Repository

Учебный репозиторий для практики основ C++.

## Структура

- `main.cpp` - основной файл для экспериментов
- `CMakeLists.txt` - настройки сборки через CMake

## Как собирать

```bash
mkdir build && cd build
cmake ..
cmake --build .
./MisherinGame
